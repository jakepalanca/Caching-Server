name: Deploy to EC2

# Trigger the workflow on push events to the main branch
on:
  push:
    branches:
      - main  # Automatically triggers on updates to the 'main' branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Set up SSH
      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.INSTANCE_SSH }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ec2-18-212-152-50.compute-1.amazonaws.com >> ~/.ssh/known_hosts

      # Step 3: SSH into the EC2 instance and deploy the application
      - name: Deploy to EC2
        run: |
          ssh -i ~/.ssh/id_rsa ec2-user@ec2-18-212-152-50.compute-1.amazonaws.com << 'EOF'
          # Your deployment commands here
          echo "Deploying application to EC2"
          cd /home/ec2-user/Caching-Server
          git pull origin main
          mvn clean install -DskipTests=false
          sudo systemctl restart caching-server.service
          EOF
